<!DOCTYPE html>
<html>
<head>
  <title>{{title}}</title>
  <link href="/css/style.css" rel="stylesheet" />
</head>

<body class="flex flex-col min-h-screen">
    <header class="sticky top-0 z-40 w-full border-b bg-white/95 backdrop-blur supports-[backdrop-filter]:bg-white/60 shadow-lg shadow-green-500/20">
        <div class="container mx-auto flex h-16 items-center justify-between px-4">
            <div class="flex items-center gap-2">
            <a href="/" class="text-xl font-bold tracking-tight text-green-600 ">
                DanceHub
            </a>
            </div>

            <nav class="hidden md:flex gap-6">
            <a href="/" class="text-sm font-medium transition-colors hover:text-green-600">
                Home
            </a>
            <a href="/#courses" class="text-sm font-medium transition-colors hover:text-green-600">
                Courses
            </a>
            {{#isInstructor}}
                <a href="/add-course" class="text-sm font-medium transition-colors hover:text-green-600">
                Add Course
                </a>
            {{/isInstructor}}
            </nav>
            {{#user}}
                <div class="flex items-center gap-4">
                <span class="text-sm font-medium text-green-700">{{user}}</span>
                <span class="text-sm font-medium text-green-700">
                ({{#isInstructor}}Instructor{{/isInstructor}}{{^isInstructor}}Student{{/isInstructor}})
                </span>


                <form action="/logout" method="POST">
                    <button type="submit" class="px-4 py-2 text-sm text-white bg-green-600 rounded hover:bg-green-700 shadow-lg shadow-green-500/20">
                    Logout
                    </button>
                </form>
                </div>
                {{/user}}

                {{^user}}
                <div class="flex items-center gap-4">
                <a href="/login" class="hidden md:flex px-4 py-2 text-sm border border-gray-300 rounded hover:bg-gray-100">
                    Log in
                </a>
                <a href="/signup" class="px-4 py-2 text-sm text-white bg-green-600 rounded hover:bg-green-700 shadow-lg shadow-green-500/20">
                    Sign up
                </a>
                </div>
                {{/user}}

        </div>
    </header>

    <main class="container mx-auto p-4">
        

        {{{ body }}}
    </main>

    <footer class="text-center text-sm py-4 text-gray-500">
        &copy; 2025 Dance Academy. All rights reserved.
    </footer>
</body>
</html>
